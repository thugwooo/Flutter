import 'package:flutter/material.dart';

class RankPoster extends StatelessWidget {
  const RankPoster({Key? key, required this.rank, required this.posterUrl})
      : super(key: key);
  final String rank;
  final String posterUrl;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
